machine:
  python:
    version: 3.6.1

dependencies:
  pre:
    - pip install -r test_requirements.txt

test:
  override:
    - ./scripts/run_tests.sh

deployment:
  staging:
    branch: master
    heroku:
      appname: rd-cms
