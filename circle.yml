machine:
  python:
    version: 3.6.1
  environment:
    SECRET_KEY: fordevandtestdoesnotmatter
    REPOS_DIRECTORY: /home/ubuntu/rd_cms/
    GITHUB_ACCESS_TOKEN: fordevandtestdoesnotmatter

dependencies:
  pre:
    - pip install -r test_requirements.txt

test:
  override:
    - ./scripts/run_tests.sh

deployment:
  staging:
    branch: master
    heroku:
      appname: rd-cms
